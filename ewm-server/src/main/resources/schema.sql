

CREATE TABLE IF NOT EXISTS ViewStats (
                                         app VARCHAR(512) NOT NULL,
                                         uri VARCHAR(512) NOT NULL,
                                         hits BIGINT
);